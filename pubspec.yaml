name: play_on_task
description: PlayOn Task

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4


  #Service Locator
  get_it: ^7.2.0

  #UI Layer
  flutter_bloc: ^7.3.3
  flutter_svg: ^0.23.0+1
  cached_network_image: ^3.1.0+1

  #Domain
  dartz: ^0.10.0
  equatable: ^2.0.3

  #Data
  connectivity_plus: ^2.0.2

  #Data - Remote
  url_launcher: ^6.0.12
  dio: ^4.0.1
  firebase_core: ^1.10.0
  firebase_auth: ^3.2.0

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/